class Main inherits IO {
   x : Int <- 5;
   y : Int <- 7;
   z : Int <- 1;
   main(): SELF_TYPE {
   {
      y <- (-9);
      if (y < z) then
	       z <- (x+y + 6) / (z - x) * (9 + 7)
      else z <- 12 fi;
	   out_string("Hello, World.\n");
   }
   };
};
