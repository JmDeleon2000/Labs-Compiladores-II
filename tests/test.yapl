class Main inherits IO {
   x : Int <- 5;
   y : Int <- 7;
   z : Int <- 1;
   main(): SELF_TYPE {
   {
      while (z < y) loop
	       z <- x+y
      pool;
	   out_string("Hello, World.\n");
   }
   };
};
